const calculator = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b
  };

  const resultAdd = calculator.add(10, 5); // 15
const resultSubtract = calculator.subtract(10, 5); // 5
const resultMultiply = calculator.multiply(10, 5); // 50
const resultDivide = calculator.divide(10, 5); // 2
